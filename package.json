{
  "name": "Made With Emergentolio-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && cd frontend && npm install",
    "build": "npm install --prefix backend && cd frontend && npm install && NODE_ENV=production npm run build",
    "start": "node backend/src/server.js",
    "render-build": "npm install --prefix backend && cd frontend && npm install && NODE_ENV=production npm run build"
  }
}