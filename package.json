{
  "name": "portfolio-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && cd frontend/frontend && npm install",
    "build": "cd frontend/frontend && npm install && npm run build",
    "start": "NODE_ENV=production node backend/src/server.js",
    "render-build": "npm install --prefix backend && cd frontend/frontend && npm install && npm run build"
  }
}
